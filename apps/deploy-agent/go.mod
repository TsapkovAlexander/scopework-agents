module tracedocs.ru/deploy-agent

go 1.26

// Зависимостей намеренно нет. Агент живёт на чужом сервере, и каждая
// внешняя библиотека — это и вес бинаря, и лишний источник уязвимостей,
// за которым придётся следить на всех подключённых хостах.
